"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getExpressionData = getExpressionData;

var _parseDataId = require("../../functions/parseDataId");

function getExpressionData(target, fileData) {
  if (target.dataset.locatorjsId) {
    const [, id] = (0, _parseDataId.parseDataId)(target.dataset.locatorjsId);
    const expData = fileData.expressions[Number(id)];

    if (expData) {
      return expData;
    }
  }

  return null;
}