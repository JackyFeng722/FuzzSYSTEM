"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getLinkTypeOrTemplate = void 0;
exports.linkTemplateUrl = linkTemplateUrl;

const getLinkTypeOrTemplate = (targets, options, localLinkTypeOrTemplate) => localLinkTypeOrTemplate || document.documentElement.dataset.locatorTarget || options.getOptions().templateOrTemplateId || Object.entries(targets)[0][0];

exports.getLinkTypeOrTemplate = getLinkTypeOrTemplate;

function linkTemplateUrl(targets, options, localLinkTypeOrTemplate) {
  const templateOrType = getLinkTypeOrTemplate(targets, options, localLinkTypeOrTemplate);
  const target = targets[templateOrType];

  if (target) {
    return target.url;
  }

  return templateOrType;
}